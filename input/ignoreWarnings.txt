== Suppressed Messages ==

# One stanza per suppressed warning. Add the literal warning text below,
# preceded by a `# Reason: <why we are suppressing this>` comment.
# See: https://confluence.hl7.org/display/FHIR/Implementation+Guide+Parameters

# (No suppressions needed. The former `Unknown_Code_in_Version` suppression for
# the `tab` itemControl code is obsolete: `tab` is now a defined code in the
# IG's tiro-item-control CodeSystem, so it validates cleanly.)
